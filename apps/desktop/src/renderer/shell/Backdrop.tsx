/**
 * The living backdrop (PRD §7 / mockup direction B): two drifting radial washes
 * driven by `--wash-a` / `--wash-b` (34s / 42s). The per-video dominant-colour
 * re-tint lands in Phase 1 (P1-6); here the washes follow the accent theme.
 */
export function Backdrop() {
  return <div className="backdrop" aria-hidden="true" />;
}
