export function HomeRoute() {
  return (
    <section className="route">
      <p className="route__kicker">Home</p>
      <h1 className="route__title">Search, then watch.</h1>
      <p className="route__note">
        A locally composed feed — continue watching, latest from followed channels, and
        because-you-watched — lands in Phase 2 (decision A1). For now, the search field above is the
        way in.
      </p>
    </section>
  );
}
